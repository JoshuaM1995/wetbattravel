import React from 'react';
import {Card, CardContent, CardHeader} from '@material-ui/core';

const ViewQuote = () => {
  return (
    <Card variant="outlined">
      <CardHeader title="View Quote" />
      <CardContent>
        Testing
      </CardContent>
    </Card>
  );
};

export default ViewQuote;
